# Core data handling and plotting
numpy>=1.26
pandas>=2.2
matplotlib>=3.8
scipy>=1.11

# Time series (Part 1 decomposition/stationarity tests, Part 4 SARIMAX)
statsmodels>=0.14

# Feature-based model (Part 6)
scikit-learn>=1.4
xgboost>=2.0

# Foundation model (Part 7) — pulls in torch and transformers as
# sub-dependencies; CPU-only is sufficient, no GPU/CUDA required
chronos-forecasting>=1.4
torch>=2.2

# Notebook execution (only needed to run the .ipynb files locally;
# not required on Colab, which provides Jupyter itself)
jupyter>=1.0
nbformat>=5.10
nbclient>=0.10

# Tests (Part 11 / tests/)
pytest>=8.0
